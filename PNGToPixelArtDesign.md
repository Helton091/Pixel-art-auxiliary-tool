# PNG 转像素风设计方案

## 1. 功能目标

本功能用于将一张高分辨率 PNG 图像自动转换为低分辨率像素风 PNG 图像，例如：

- 输入：`1000x1000`、`2048x2048` 等高清图片
- 输出：`20x20`、`32x32`、`64x64` 等低分辨率像素图

目标不是简单缩小图片，而是生成具有明确块面、轮廓感和像素风格的结果，适合：

- 游戏素材
- 图标生成
- 像素画预处理
- 足球、角色头像、UI 图标等题材的风格化转换

---

## 2. 核心思路

这项功能的本质是“降采样 + 风格化重建”，而不是普通的图片缩放。

推荐的处理链路如下：

1. 读取原始 PNG
2. 根据目标尺寸划分输出网格
3. 将原图按网格分块映射到目标像素
4. 对每个块进行代表色计算
5. 对结果做颜色简化或抖动处理
6. 可选执行边缘增强
7. 输出像素风 PNG

---

## 3. 为什么不能只做普通缩放

如果直接使用线性插值或双线性缩放，结果通常会出现：

- 细节被平滑掉
- 边缘模糊
- 色彩过渡太自然，不像像素风
- 输出更像“缩小版照片”，而不是“像素图”

如果使用简单的点采样，又容易出现：

- 采样不稳定
- 边缘锯齿严重
- 局部颜色失真
- 视觉噪声较多

因此需要在“稳定性”和“像素风格”之间做更合适的折中。

---

## 4. 推荐算法路线

### 4.1 基础版：区域平均采样

对输出图的每一个像素块，取原图对应区域内的平均颜色。

优点：

- 实现简单
- 结果稳定
- 适合大面积色块
- 可作为默认模式

缺点：

- 容易失去边缘
- 轮廓会变软
- 图像可能偏“糊”

建议作为第一版默认算法。

---

### 4.2 增强版：区域平均 + 颜色量化

在平均采样之后，将颜色限制到较少的调色板中，例如：

- 2 色
- 4 色
- 8 色
- 16 色

这会让结果更接近像素风，而不是普通缩小图。

适合：

- 足球图标
- 角色头像
- 卡通风素材
- 游戏 UI 图标

---

### 4.3 进阶版：边缘增强 + 量化

在生成低分辨率图前，先对原图做边缘检测或轮廓强调，然后把边缘信息融合进最终结果。

可选策略：

- Sobel / Laplacian 边缘检测
- 先轻微模糊再检测边缘，减少噪点
- 在输出网格中对边缘区域赋予更高权重

效果：

- 主体轮廓更硬
- 足球黑白拼块更清楚
- 结果更像“像素风图标”

---

### 4.4 高级版：抖动处理

当输出图的颜色数很少时，可以加入抖动（dithering）：

- Floyd–Steinberg
- Ordered Dithering

作用：

- 减少色带
- 让渐变更自然
- 在有限调色板下提升观感

建议作为可选项，不强制开启。

---

## 5. 建议的默认模式

如果目标是“效果好且实现成本适中”，建议默认使用：

**区域平均采样 + 线性颜色空间平均 + 可选调色板量化**

原因：

- 区域平均稳定
- 在线性空间平均比直接在 sRGB 中平均更合理
- 调色板量化能明显增强像素风格

这是最适合作为第一版发布的方案。

---

## 6. 面向足球图片的专门优化

如果输入图像是足球、球类或结构清晰的图标，可以进一步优化：

### 6.1 主体识别

先尝试识别主体区域，例如足球本体，再对背景和主体分别处理。

背景可以更粗略：

- 更低分辨率
- 更少颜色
- 更强压缩

主体则保留更多轮廓信息。

---

### 6.2 黑白块增强

足球类图片通常有明显的黑白块结构，可以对高对比区域进行强化：

- 增加阈值化步骤
- 把接近黑白的颜色直接映射到黑/白
- 保留少量灰色作为过渡

这样更容易得到类似示例图的效果。

---

### 6.3 轮廓优先

对于圆形主体，轮廓清晰比内部纹理更重要。可以让边界像素优先保留对比度，而内部纹理做更强简化。

---

## 7. 数据流设计

建议将这项功能拆成独立流程：

### 输入阶段

- 选择 PNG 文件
- 读取为 RGBA 图像
- 校验尺寸与格式

### 处理阶段

- 确定目标宽高
- 计算分块映射
- 统计每块颜色
- 执行量化/抖动/边缘增强

### 输出阶段

- 生成新的 `CanvasDocument`
- 在画布中预览
- 导出为 PNG
- 也可复用现有 C++ 导出功能

---

## 8. 与现有项目的集成方式

当前项目已经有以下基础能力：

- PNG 导入
- PNG 导出
- 像素文档模型 `CanvasDocument`
- 画布渲染与缩放
- 选区与裁剪

因此新功能最适合以“导入后转换”方式集成：

1. 用户导入 PNG
2. 在右侧新增“像素化转换”功能区
3. 用户设置目标尺寸与风格模式
4. 点击生成
5. 画布切换到新的像素化结果
6. 允许再导出 PNG 或 C++ 代码

这样不会破坏原有绘图流程，也能和现有导入导出体系无缝衔接。

---

## 9. 推荐 UI 方案

建议在当前工具里增加一个独立面板：

### 基本参数

- 目标宽度
- 目标高度
- 采样模式
- 调色板数量
- 是否启用边缘增强
- 是否启用抖动

### 预设按钮

- `Icon 16x16`
- `Pixel Art 32x32`
- `Poster 64x64`
- `Football Style`

### 操作按钮

- `Load PNG`
- `Generate`
- `Preview`
- `Export PNG`
- `Export C++`

---

## 10. 推荐实现模块

建议新增一个独立模块，例如：

- `ImagePixelizer.h`
- `ImagePixelizer.cpp`

职责：

- 输入 `Image` 或原始 RGBA 缓冲
- 输出低分辨率像素数组
- 提供多种转换模式
- 与 `CanvasDocument` 对接

这样可以避免把算法直接堆进 `CanvasApp.cpp`，保持代码结构清晰。

---

## 11. 建议的第一版功能范围

第一版不必一次做太复杂，推荐范围如下：

### 必做

- PNG 导入
- 目标宽高设置
- 区域平均采样
- 输出到 `CanvasDocument`
- PNG 导出预览

### 可选

- 调色板量化
- 边缘增强
- 抖动
- 预设按钮

### 暂缓

- 主体分割
- 复杂 AI 辅助识别
- 自动风格学习

---

## 12. 风险与注意事项

### 12.1 色彩空间

如果直接在 sRGB 中平均颜色，结果可能偏暗。更稳妥的做法是：

- sRGB -> 线性空间
- 在线性空间平均
- 再转回 sRGB

---

### 12.2 透明通道

PNG 可能带透明度，需要单独处理 alpha：

- 透明像素不要直接参与普通颜色平均
- 或者按 alpha 加权平均

---

### 12.3 极端比例图片

原图和目标尺寸比例差异很大时，可能出现：

- 细节过度损失
- 主体变形
- 背景占比失衡

需要考虑是否支持：

- 自动裁剪主体
- 保持宽高比
- 填充留白

---

### 12.4 性能

对大图进行块状统计时，注意避免过多重复遍历。

建议：

- 使用一次性分块统计
- 或对图像做积分图 / 前缀和优化
- 不要在 UI 线程里做过重计算时卡死界面

---

## 13. 最终推荐结论

这项“PNG 转像素风”功能，最值得做成：

- 以**区域平均采样**为基础
- 以**颜色量化**为风格化核心
- 以**边缘增强**为观感加分项
- 以**足球类图像的轮廓和黑白块结构**作为重点优化对象

如果要做成项目里真正的“王炸功能”，建议把它设计成一个独立的图像风格化工作流，而不是单纯的缩放导出。

---

## 14. 目标效果定义

理想输出应满足：

- 一眼能看出主体是什么
- 轮廓清晰
- 色彩简洁
- 像素块感明显
- 放大后依然像有风格的像素图，而不是普通缩略图

这才是本功能真正的价值。
